<div class="page-header">
    <h1>Last games</h1>
</div>
{% if lastgames %}
<div class="table-responsive">
    <table class="table table-hover table-condensed">
        <thead>
            <tr>
                <th>Date</th>
                <th>Hour</th>
                <th>Local</th>
                <th>Visitor</th>
                <th>Result</th>
            </tr>
        </thead>
    	<tbody>
    	{% for lastgame in lastgames %}
            <tr onclick="document.location = '{{ path("ecucurella_SporTiu_games_id", {id: lastgame.id}) }}';">
    		    <td>{{ lastgame.gamedate|date('d-m-Y') }}</td>
    		    <td>{{ lastgame.gamedate|date('H:i') }}</td>
    		{% if lastgame.localpoints == lastgame.visitorpoints %}
                <td class="warning">{{ lastgame.localclub.name}}</td>
                <td class="warning">{{ lastgame.visitorclub.name}}</td>
    		{% elseif lastgame.localpoints > lastgame.visitorpoints %}
                <td class="success">{{ lastgame.localclub.name}}</td>
                <td class="danger">{{ lastgame.visitorclub.name}}</td>	
    		{% else %}
                <td class="danger">{{ lastgame.localclub.name}}</td>
                <td class="success">{{ lastgame.visitorclub.name}}</td>        		
    		{% endif %}
    			<td><strong>{{ lastgame.localpoints}} - {{ lastgame.visitorpoints}}<strong></td>
            </tr>
    	{% endfor %}
        </tbody>
    </table>
</div>
{% else %}
<div class="alert alert-info">
    There is no played games in database yet !!
</div>
{% endif %}